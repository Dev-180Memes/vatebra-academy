const username = "Adeoluwa";

function printName(name) {
    console.log(name)
}

printName(username);

// Path: index.js

// Create a function that takes in a number as a parameter and returns the square of that number

function squareNumber(number) {
    return number * number;
}

console.log(squareNumber(2));